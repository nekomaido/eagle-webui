services:
  eagle-webui:
    build: .
    image: eagle-webui:latest
    container_name: eagle-webui
    ports:
      - "34917:34917"
    volumes:
      # Mount the parent folder that contains one or more *.library directories.
      - ${EAGLE_ROOT:-./eagle}:/eagle:ro
    restart: unless-stopped
